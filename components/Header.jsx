import Link from 'next/link'; import { site } from '../content/site'; import MobileNav from './MobileNav';
export default function Header(){ return (<header className="sticky top-0 z-50 border-b bg-white/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-4"><Link href="/" className="text-xl font-bold tracking-wide">{site.name}</Link><nav className="hidden gap-6 md:flex">{site.nav.map(([l,h])=><Link key={l} href={h} className="text-sm text-gray-700 hover:text-ink">{l}</Link>)}</nav><div className="flex items-center gap-3"><Link href="/contact" className="hidden rounded-full bg-ink px-5 py-2 text-sm text-white md:inline-block">Réserver</Link><MobileNav/></div></div></header>); }