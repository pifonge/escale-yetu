"use client"; import { useState } from 'react'; import Link from 'next/link'; import { Menu, X } from 'lucide-react'; import { site } from '../content/site';
export default function MobileNav(){ const [open,setOpen]=useState(false); return (<div className="md:hidden"><button onClick={()=>setOpen(v=>!v)} className="rounded-full border p-2">{open ? <X size={18}/> : <Menu size={18}/>}</button>{open && <div className="absolute left-0 right-0 top-16 border-b bg-white p-4 shadow-soft"><div className="grid gap-3">{site.nav.map(([l,h])=><Link key={l} href={h} onClick={()=>setOpen(false)} className="rounded-xl px-3 py-2 hover:bg-sand">{l}</Link>)}</div></div>}</div>); }