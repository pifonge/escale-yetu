import Header from '../../components/Header'; import Footer from '../../components/Footer'; import SectionTitle from '../../components/SectionTitle'; import { site } from '../../content/site';
export default function Menu(){ return (<main><Header/><section className="mx-auto max-w-5xl px-6 py-16"><SectionTitle eyebrow="Menu" title="Une carte généreuse et lisible" text="Les prix et catégories sont issus du menu fourni et pensés pour une lecture claire sur mobile."/>{site.menuSections.map(s => (<div key={s.title} className="mt-10"><h2 className="text-2xl font-semibold">{s.title}</h2><div className="mt-4 grid gap-3">{s.items.map(([n,p]) => (<div key={n} className="flex items-center justify-between rounded-2xl border p-4"><span>{n}</span><span className="font-semibold">{p}</span></div>))}</div></div>))}</section><Footer/></main>); }