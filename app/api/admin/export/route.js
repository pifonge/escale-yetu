import { isAdminSession } from '../../../../lib/auth'; import { site } from '../../../../content/site'; import { exportJSON } from '../../../../lib/backup'; export async function GET(req){ if (!isAdminSession(req)) return Response.json({ ok:false, error:'Unauthorized' }, { status: 401 }); return new Response(exportJSON({ site }), { headers: { 'Content-Type': 'application/json' } }); }