export async function postToOdoo(endpoint, payload) { const baseUrl = process.env.ODOO_BASE_URL; const apiKey = process.env.ODOO_API_KEY; if (!baseUrl || !apiKey) return { ok: false, error: 'Odoo not configured' }; const res = await fetch(`${baseUrl.replace(/\/$/, '')}/${endpoint.replace(/^\//, '')}`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${apiKey}` }, body: JSON.stringify(payload) }); return { ok: res.ok, data: await res.json().catch(() => ({})) }; }
